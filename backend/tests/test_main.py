# =============================================================================
#                                  main.py
# =============================================================================


# root ========================================================================
# !. read root success


def test_main_read_root(client):
    response = client.get('/')
    assert response.json() is not None
